// Pharmacy Symptom Search addon — pharmacist review queue (PRD §8, §9).
// Thin page; all client logic lives in ./_ui (house page/_ui split).
import PharmacyReviewQueue from './_ui';

export default function PharmacyReviewsPage() {
  return <PharmacyReviewQueue />;
}
