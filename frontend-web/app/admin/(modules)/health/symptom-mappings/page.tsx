// Pharmacy Symptom Search addon — mapping approval workbench (PRD §4 suggest-approve).
// Thin page; all client logic lives in ./_ui (house page/_ui split).
import SymptomMappingWorkbench from './_ui';

export default function SymptomMappingsPage() {
  return <SymptomMappingWorkbench />;
}
