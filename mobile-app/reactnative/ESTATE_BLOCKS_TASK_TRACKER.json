{
  "project": "Estate super-app — Blocks 29–47 (property/dues/meetings/tasks/maintenance/facilities/announcements/emergencies/documents/finance/admin/vendor/notifications/reports/settings/states/hardening)",
  "session_date": "2026-06-22",
  "note": "Durable tracker for the Estate Blocks 29–47 build (root task-tracker.json is contended by concurrent sessions). Follows the proven Visitor/Election pattern: additive migration -> /api/v1 handlers -> mobile features/<domain> + screens (dual mock/live behind USE_MOCK). Roadmap: docs/estate/ESTATE_BLOCKS_29-47_ROADMAP.md.",
  "progress": { "blocks_total": 19, "schema_done_blocks": 9, "blocks_fully_done": ["Block 29 Dues/payments money-path (mobile + 2 handlers + dues.ts service + schema)", "Block 30 Meetings (mobile + 5 handlers + schema)", "Block 31 Tasks (mobile + 3 handlers + schema)", "Block 32 Maintenance/Repairs (mobile + 3 handlers + schema)", "Block 33 Facilities (mobile + 3 handlers + schema)", "Block 34 Announcements (mobile + 3 handlers + schema)", "Block 35 Emergencies (mobile + 2 handlers + schema)", "Block 36 Documents (mobile + 2 handlers + schema)", "Block 37 Vendors+jobs (mobile + 3 handlers + schema)"], "domains_29_37_done": "9 of 9 schema-backed blocks shipped full-stack", "domains_38_46_done": "9 of 9 shipped full-stack (3 new tables for 39/43/45; rest reuse existing tables)", "blocks_29_46_complete": "18 of 18", "percent_pending": 2, "remaining": "Only Block 47d (deploy / CI-run on a GH runner / live-DB RLS+index penetration test / flip USE_MOCK) — environment-blocked. Blocks 47a (OpenAPI), 47b (money-path tests), 47c (static RLS/index audit + index hardening) are DONE.", "state_refresh_2026_06_22": { "estate_api_handlers": 34, "estate_server_services": ["ai-notes","dues","finance","reports","resident"], "mobile_estate_feature_modules": 18, "estate_route_groups": 17, "estate_migrations": 8, "additive_guard": "PASS", "openapi_estate_paths": 34, "openapi_estate_operations": 42, "dues_money_path_tests": "11 passing", "ai_notes_tests": "6 passing", "contract_check": "in-sync (42 impl ops == 42 documented), verified statically (sandbox mount flapping prevented live run; tests ran green earlier)" } },
  "completed_tasks": [
    { "id": "be-dues", "title": "Block 29 Dues — BACKEND money-path: src/server/estate/dues.ts (listInvoices read-time overdue projection; payInvoice reuses shared debitWallet primitive = atomic debit_wallet_atomic RPC + fail-closed tier limit via enforceWalletLimit + immutable ledger entry w/ metadata {kind:estate_dues,estate_id,invoice_id,category,payer_id}; records estate_payments audit row keyed by Idempotency-Key; marks invoice paid; idempotent fast-path returns prior payment). Handlers: GET /estate/dues, POST /estate/dues/[id]/pay (requires Idempotency-Key header, 400 if missing).", "verified": "imports resolve (debitWallet, getResidentContext, errorResponse); kobo integers; never UPDATEs balance; idempotency-key required" },
    { "id": "mobile-dues", "title": "Block 29 Dues — MOBILE module: src/features/dues (types, CATEGORY_META/STATUS_META, dual mock/live api with idempotency on pay, hooks) + app/dues (outstanding-balance summary card + invoice list w/ overdue projection + Pay-from-wallet confirm). Registered route + Dues & Rent tile (financial).", "verified": "static checks: tokens only in screen; icons resolve; route + tile registered" },
    { "id": "be-emergencies", "title": "Block 35 Emergencies — BACKEND: handlers at frontend-web/app/api/v1/estate/emergencies (GET list estate-scoped, POST create [kind-validated, reporter=user], [id]/resolve POST [estate-ownership checked]). Uses getResidentContext + resolveNames; maps estate_emergency_alerts.", "verified": "imports cross-checked vs tasks template; 2 route files; estate ownership guard on resolve" },
    { "id": "mobile-emergencies", "title": "Block 35 Emergencies — MOBILE module: src/features/emergencies (types, dual mock/live api with KIND_META/STATUS_META, hooks with idempotency on create) + app/emergencies screens (index list with status chips + prominent panic CTA + resolve action; report.tsx kind grid + location/description + danger submit). Registered route group + Emergencies tile. Added PrimaryButton 'danger' variant (additive).", "verified": "static checks: no raw hex outside api constants; all lucide icons resolve; route + tile registered" },
    { "id": "be-announcements", "title": "Block 34 Announcements — BACKEND: handlers at frontend-web/app/api/v1/estate/announcements (GET list w/ per-user read state via announcement_reads, POST create admin-only, GET [id] + upsert read on UNIQUE(announcement_id,user_id), POST [id]/read 204).", "verified": "imports cross-checked; 3 route files; admin gate on create; onConflict matches migration UNIQUE" },
    { "id": "mobile-announcements", "title": "Block 34 Announcements — MOBILE module: src/features/announcements (types, KIND_META, dual mock/live api, hooks) + app/announcements screens (index w/ unread dot + kind icons, [id] detail marks read, create admin kind selector). Registered route + tile.", "verified": "static checks: tokens only; icons resolve; route + tile registered" },
    { "id": "be-tasks", "title": "Block 31 Tasks — BACKEND: handlers at frontend-web/app/api/v1/estate/tasks (GET list, POST create, GET [id], POST [id]/status). Uses getResidentContext + resolveNames; maps estate_tasks.", "verified": "imports cross-checked; 3 route files" },
    { "id": "mobile-tasks", "title": "Block 31 Tasks — MOBILE module: src/features/tasks (types, dual mock/live api with TaskColors/PRIORITY_META, hooks) + app/tasks screens (index segmented todo/in_progress/done, create, [id] with status update buttons). Registered route + tile.", "verified": "static checks: tokens only; icons resolve; route + tile registered" },
    { "id": "be-repairs", "title": "Block 32 Maintenance/Repairs — BACKEND: handlers at frontend-web/app/api/v1/estate/repairs (GET list, POST create [category+urgency validated], GET [id] + activity timeline from repair_updates, POST [id]/updates [inserts repair_update + advances request status, estate-ownership checked]). Shared mapRepair exported from route.ts.", "verified": "imports cross-checked; 3 route files; ownership guard on detail/update" },
    { "id": "mobile-repairs", "title": "Block 32 Maintenance/Repairs — MOBILE module: src/features/repairs (types, CATEGORY_META/URGENCY_META/STATUS_META + STATUS_FLOW, dual mock/live api w/ idempotency, hooks) + app/repairs (index list w/ status+urgency chips, report.tsx category grid + urgency + description, [id] detail w/ activity timeline + advance-status CTA). Registered route + Maintenance tile.", "verified": "static checks: tokens only; icons resolve; route + tile registered" },
    { "id": "be-facilities", "title": "Block 33 Facilities — BACKEND: handlers at frontend-web/app/api/v1/estate/facilities (GET list facilities; bookings GET [my reservations], POST [reserve — requires Idempotency-Key, validates future window, captures fee_kobo, status pending if fee>0 else confirmed], bookings/[id]/cancel POST [owner-checked]). Booking is a reservation; settlement deferred to dues money-path. Shared mapBooking exported.", "verified": "imports cross-checked; 4 route files; idempotency-key required on create; future-window + ownership guards" },
    { "id": "mobile-facilities", "title": "Block 33 Facilities — MOBILE module: src/features/facilities (types, KIND_META/BOOKING_STATUS_META, dual mock/live api w/ idempotency, hooks) + app/facilities (index Browse/My-bookings tabs w/ fee + cancel, book.tsx day/time-slot/duration presets reusing meetings pattern + fee CTA). Registered route + Facilities tile.", "verified": "static checks: tokens only; icons resolve; route + tile registered" },
    { "id": "be-documents", "title": "Block 36 Documents — BACKEND: handlers at frontend-web/app/api/v1/estate/documents (GET list — restricted docs filtered to estate_admin only via ctx.role; POST create admin-only, validates https file URL). Maps estate_documents.", "verified": "imports cross-checked; 1 route file w/ GET+POST; restricted gating by role" },
    { "id": "mobile-documents", "title": "Block 36 Documents — MOBILE module: src/features/documents (types, CATEGORY_META, dual mock/live api, hooks) + app/documents (index horizontal category filter + Restricted badge + open via Linking, upload.tsx admin title/url/category/restricted form). Registered route + Documents tile.", "verified": "static checks: tokens only; icons resolve; route + tile registered" },
    { "id": "be-vendors", "title": "Block 37 Vendors+jobs — BACKEND: handlers at frontend-web/app/api/v1/estate/vendors (GET directory ordered by rating), vendors/jobs (GET list w/ vendor names), vendors/jobs/[id]/status (POST advance job status, estate-ownership checked). Shared mapJob exported.", "verified": "imports cross-checked; 3 route files; ownership guard on status" },
    { "id": "mobile-vendors", "title": "Block 37 Vendors+jobs — MOBILE module: src/features/vendors (types, VENDOR_CATEGORY_META/VENDOR_STATUS_META/JOB_STATUS_META, dual mock/live api, hooks) + app/vendors (Directory/Jobs tabs: vendor cards w/ rating star + verified chip + tap-to-call via Linking; job cards w/ status + amount). Registered route + Vendors tile.", "verified": "static checks: tokens only; icons resolve; route + tile registered" },
    { "id": "be-meetings", "title": "Block 30 Meetings — BACKEND: 5 Supabase-direct handlers at frontend-web/app/api/v1/estate/meetings (GET list, POST create [auto-RSVPs creator], GET [id], POST [id]/rsvp [upsert on UNIQUE(meeting_id,user_id)], GET [id]/minutes) + src/server/meetings/meetings.service.ts (mapMeeting: rsvp counts, myRsvp, createdByName; status enum already matches client). CI scoped tsconfig + workflow paths extended to cover meetings.", "verified": "imports cross-checked; 4 route files; onConflict matches migration UNIQUE; workflow YAML + tsconfig valid" },
    { "id": "mobile-meetings", "title": "Block 30 Meetings — MOBILE module: src/features/meetings (types, constants incl. MeetingColors + USE_MOCK + /api/v1/estate/meetings base, mock, dual mock/live api, utils, hooks, MeetingCard) + app/meetings screens (index list upcoming/past, create with mode/start/duration, [id] detail + RSVP yes/maybe/no + responses bars + minutes). Registered route + Meetings module tile. Reuses ScreenHeader/StateView/PrimaryButton/TextInputField/MetricBar.", "verified": "static checks: no hardcoded colors outside constants; all lucide icons resolve; route + tile registered; 11 files" },
    { "id": "roadmap", "title": "Roadmap for Blocks 29–47 (domain → tables/screens/endpoints/status) at docs/estate/ESTATE_BLOCKS_29-47_ROADMAP.md", "verified": "authored" },
    { "id": "schema-29-37", "title": "Data foundation: additive migration 20260622010000_estate_modules.sql — 16 tables across Blocks 29–37 (estate_dues_invoices, estate_payments, estate_meetings, meeting_rsvps, meeting_minutes, estate_tasks, estate_repair_requests, repair_updates, estate_facilities, facility_bookings, estate_announcements, announcement_reads, estate_emergency_alerts, estate_documents, estate_vendors, vendor_jobs). Estate-scoped RLS + service-role bypass via DO-block loop; money in kobo; 11 indexes.", "verified": "additive-only guard PASS (all 3 visitor/estate migrations); 16 CREATE TABLE; YAML CI updated to guard estate_modules" }
  ],
  "completed_tasks_38_46": [
    { "id": "schema-38-46", "title": "Additive migration 20260622020000_estate_modules_38_46.sql — 3 NEW tables (estate_ai_notes, estate_notifications, estate_member_settings). Estate-scoped RLS for ai_notes; owner-scoped (user_id=auth.uid) RLS for notifications + settings; service-role bypass; partial unread index. Blocks 38/40/41/42/44/46 reuse existing tables (estate_properties, estate_payments, vendor_jobs, estate_repair_requests, estate_meetings).", "verified": "additive guard PASS; 3 CREATE TABLE; CI migration-guard + globs updated" },
    { "id": "block-38-property", "title": "Block 38 Property management — MOBILE src/features/properties + app/properties (occupancy summary stats, occupied/vacant/reserved filter, landlord/tenant) + BACKEND GET /estate/properties over existing estate_properties with occupancy summary + name resolution.", "verified": "tokens only; icons resolve; reuses estate_properties from 20260618000000_estate_onboarding.sql" },
    { "id": "block-39-ainotes", "title": "Block 39 AI meeting notes — MOBILE src/features/ainotes + app/ai-notes (summary + action-item cards) + BACKEND GET /estate/ai-notes, POST /estate/ai-notes/generate (admin-only) using src/server/estate/ai-notes.ts — a deterministic, dependency-free EXTRACTIVE summariser (TF-scored sentences + positional bias) over meeting_minutes; action items lifted from decisions[] + imperative lines. No external LLM.", "verified": "summariser is pure/reproducible; admin gate; reads meeting_minutes" },
    { "id": "block-40-finance", "title": "Block 40 Finance dashboard — MOBILE src/features/finance + app/finance (collected this-month/all-time hero, outstanding + collection-rate stats, by-category bars, recent payments) + BACKEND GET /estate/finance (admin-only) via src/server/estate/finance.ts aggregating estate_payments + estate_dues_invoices (kobo).", "verified": "read-only aggregation; admin gate; no new tables" },
    { "id": "block-41-admin", "title": "Block 41 Admin panel — MOBILE src/features/estateadmin + app/estate-admin (residents/properties/meetings stats, needs-attention list, 12 quick-action tiles with attention badges) + BACKEND GET /estate/admin/summary (admin-only) counting join requests/emergencies/repairs/invoices/bookings/meetings via head:true counts.", "verified": "admin gate; count-only queries; deep-links to existing routes" },
    { "id": "block-42-vendorapp", "title": "Block 42 Vendor self-service — MOBILE app/vendor-portal (Active/Completed tabs, per-status lifecycle action buttons accept/decline/start-travel/begin-work/complete) reusing src/features/vendors (added NEXT_JOB_ACTIONS map) + existing vendor_jobs status handler. No new backend.", "verified": "reuses vendor_jobs + updateJobStatus; tokens only" },
    { "id": "block-43-notifications", "title": "Block 43 Notifications center — MOBILE src/features/notifications + app/estate-notifications (unread styling + dot, category icons, tap=mark-read+deeplink, mark-all) + BACKEND GET /estate/notifications (owner-scoped), POST [id]/read, POST read-all over estate_notifications.", "verified": "owner-scoped (user_id) queries; 3 route files" },
    { "id": "block-44-reports", "title": "Block 44 Reports — MOBILE src/features/reports + app/reports (report-section cards w/ metric grid) + BACKEND GET /estate/reports (admin-only) via src/server/estate/reports.ts computing dues-collection, payments-by-method, maintenance-throughput, meetings sections.", "verified": "read-only; admin gate; no new tables" },
    { "id": "block-45-settings", "title": "Block 45 Settings — MOBILE src/features/estatesettings + app/estate-settings (channel + per-category notification toggles w/ optimistic update + master-off gating) + BACKEND GET/PATCH /estate/settings over estate_member_settings (camel↔snake map, partial-PATCH merges onto existing row so it never wipes other prefs, upsert on (estate_id,user_id)).", "verified": "PATCH merges existing before upsert (no clobber); optimistic + rollback in hook" },
    { "id": "block-46-states", "title": "Block 46 Empty/error states — verified every NEW list/detail screen (38–46) renders StateView loading+error+empty; dashboards (finance/admin/reports/settings) render error+content (no list ⇒ no empty). No raw hex; standardized retry actions.", "verified": "grep audit: all lists have kind=error+empty; dashboards error+content" },
    { "id": "register-38-46", "title": "Registered all 8 new route groups in app/_layout.tsx + 9 module tiles in modules.ts; extended tsconfig.visitorcheck.json includes + CI workflow path globs + migration-guard glob for the 38–46 domains.", "verified": "8 groups registered; tsconfig+YAML valid; 0 missing lucide icons (30 checked); additive guard PASS on both estate migrations" }
  ],
  "completed_tasks_47": [
    { "id": "block-47a-openapi", "title": "Block 47a — Authored contracts/estate.openapi.yaml (OpenAPI 3.1) covering all 34 estate paths / 42 operations with request+response schemas, idempotency params on money mutations, and admin-only 403s. Added frontend-web/scripts/contract-check.mjs (impl↔spec drift guard: enumerates route.ts HTTP exports, maps [id]→{id}, asserts 1:1 with documented paths) + npm run contract:check. Wired estate spec into CI openapi job + a new contract-check job. Mirrors the visitor.openapi.yaml module pattern.", "verified": "static: 42 implemented (METHOD path) ops == 42 documented, one-to-one; 34 path keys; no tabs" },
    { "id": "block-47b-money-tests", "title": "Block 47b — tests/unit/estate/dues.money-path.spec.ts (11 tests) + ai-notes.spec.ts (6 tests). Money-path invariants: Idempotency-Key required (400); no double-debit + no duplicate estate_payments row on replay (debitWallet not re-called, upsert onConflict=reference); reconcile path when debit alreadyProcessed but payment row missing; wrong estate/resident → 404; non-positive/non-integer kobo → 422; already-paid → 409; debitWallet called with kobo + key + estate_dues metadata; invoice-status update failure surfaces 500 (no fire-and-forget). Added npm scripts test/test:money/test:estate + estate-money-tests CI job.", "verified": "RAN GREEN in-sandbox: 17/17 estate tests pass; neighbouring wallet+tiers 82/82 still pass; lockfile untouched" },
    { "id": "block-47c-rls-audit", "title": "Block 47c — docs/estate/SECURITY-RLS-INDEX-AUDIT.md (every estate table has RLS + estate-/owner-scoped SELECT + service-role bypass; admin writes gated in code; cross-estate isolation per route). Found 5 hot paths missing indexes → added additive migration 20260622030000_estate_indexes.sql (meeting_minutes.meeting_id, repair_updates.request_id, facility_bookings(estate_id,resident_id), estate_facilities.estate_id, estate_vendors.estate_id). Wired into CI migration-guard + triggers.", "verified": "additive guard PASS (0 destructive DDL); audit cross-checked against handler query filters" },
    { "id": "block-47d-prep", "title": "Block 47d (prep — non-infra parts, all that's buildable in-sandbox): mobile-app/reactnative/.env.production.example (all 18 estate flags=false + API base URL); scripts/check-estate-mocks.mjs deploy gate (fails if any estate USE_MOCK flag is missing or != 'false', so the 'flip USE_MOCK' step can't silently miss a module); docs/estate/DEPLOY-RUNBOOK.md (ordered migrations→backend→flip-flags→money-path verify→RLS verify→mobile build/e2e→rollback, with [infra] steps tagged).", "verified": "18 estate flags enumerated from grep of all feature USE_MOCK reads; guard parses KEY=VALUE + checks API base placeholder" }
  ],
  "pending_tasks": [
    { "id": "block-47d-infra", "priority": 2, "blocked_on": "GH runner / live env", "buildable_in_sandbox": false, "title": "Block 47d — INFRA EXECUTION ONLY (last remaining item; all prep shipped): on a GH runner run tsc/expo-export + npm run test:money + contract:check; apply additive migrations (supabase db push); deploy frontend-web handlers with server-only SUPABASE_SERVICE_ROLE_KEY; create .env.production from template + run scripts/check-estate-mocks.mjs; live-DB RLS penetration test (non-service-role token) + EXPLAIN ANALYZE the 5 new indexes; EAS build + e2e smoke (idempotent dues pay, emergency, RSVP, booking, AI note, settings). Follow docs/estate/DEPLOY-RUNBOOK.md." }
  ],
  "closeout": {
    "request": "Close out all pending tasks in ESTATE_BLOCKS_TASK_TRACKER.json",
    "result": "All 9 schema-backed domains (Blocks 29–37) shipped FULL-STACK: mobile feature module (dual mock/live behind EXPO_PUBLIC_<X>_USE_MOCK) + app/<group> screens + frontend-web /api/v1/estate handlers. Block 29 dues is a true money-path (reuses debitWallet: atomic ledger debit + fail-closed tier check + Idempotency-Key + estate_payments audit row; never UPDATEs a balance). Route groups + module tiles registered; tsconfig.visitorcheck.json + CI workflow globs extended to all new domains. Static QA passed: 0 raw hex in screens, all lucide icons resolve, all backend imports resolve, JSON valid.",
    "honestly_not_closeable_here": "Blocks 38–46 require their own additive migrations first (out of the 29–37 foundation scope). Block 47 deploy/CI-run/live-DB audit is environment-blocked (no GH runner or live env in the authoring sandbox). Full project tsc/expo-export deferred to the wired GH CI."
  }
}
