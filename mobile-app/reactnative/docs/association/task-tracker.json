{
  "project": "Association / Group Membership module — React Native mobile app",
  "note": "Module-scoped tracker. The repo-root task-tracker.json is currently owned by a parallel Paymax FX session, so this file holds the Association module's state to avoid clobbering it.",
  "last_updated": "2026-06-20",
  "module": { "route_prefix": "app/association/", "feature_folder": "src/features/association/" },
  "completed_tasks": [
    { "id": "s1-contract", "title": "Member data/type contract + mock API + hooks (kobo, idempotency, USE_MOCK)" },
    { "id": "s1-components", "title": "Feature components + promoted shared QrCodeView" },
    { "id": "s1-screens", "title": "13 member screens: discovery, org detail, join, submitted, home, card, directory, member detail, dues, pay, receipt, edge states" },
    { "id": "s1-wiring", "title": "Registered 'associations' entry in src/constants/modules.ts" },
    { "id": "s1-qa", "title": "QA pass: scoped tsc 0 errors, token compliance, design-vs-code conflicts" },
    { "id": "s2-contract", "title": "Engagement contract: announcements, notifications, meetings, tasks, documents (types/mock/api/hooks/constants)" },
    { "id": "s2-announcements", "title": "Announcements feed + detail + acknowledge + read receipts" },
    { "id": "s2-notifications", "title": "Notifications center (grouped, auto mark-read, deep links)" },
    { "id": "s2-meetings", "title": "Meetings dashboard + detail (RSVP, QR/manual check-in, agenda, minutes link)" },
    { "id": "s2-tasks", "title": "Tasks list + detail (checklist, comments, status progression)" },
    { "id": "s2-documents", "title": "Documents vault + viewer (categories, AI summary, version history, restricted, acknowledge)" },
    { "id": "s2-wiring-qa", "title": "Routes registered; dashboard shortcuts/bell wired; scoped tsc 0 errors; no hex literals" },
    { "id": "s3-chat", "title": "Group chat (I): contract + ChatThreadRow/MessageBubble + inbox + message screen (posting blocks: announcement-only, role-restricted, payment-restricted, archived); dashboard Chat shortcut; scoped tsc 0 errors" },
    { "id": "s4-ainotes", "title": "AI note-taking (L): contract + dashboard + source picker + processing + review (summary/minutes/decisions/actions/people, edit summary, approve→publish gate, convert action item→task); meeting→generate-minutes link; dashboard AI-notes shortcut; scoped tsc 0 errors" },
    { "id": "s5-join-variants", "title": "Join variants (B): code validation contract + shared CodeEntryView; invite-code, access-code, QR-scan screens; required-document upload (expo-image-picker); discovery code/scan chips; join→documents link; scoped tsc 0 errors. NOTE: QrCodeView upgraded to real react-native-qrcode-svg by parallel session (INFRA QR item resolved)." },
    { "id": "s6-profile-community", "title": "Member profile (C): dashboard/edit/privacy/activity + completion checklist. Committees (N): list + detail + join request. Events (O): list + detail w/ RSVP/register/ticket-QR/check-in/feedback. +8 routes; home gains profile button + Events/Committees tiles (10-tile grid). scoped tsc 0 errors; 40 screens." },
    { "id": "s7-admin-lite", "title": "Admin-lite (Q/R/S/T): contract + dashboard KPIs; approval queue + application detail (approve/reject/request-info, SLA, idempotent); finance/treasurer + offline-payment approvals; bulk upload intro→preview→result. +7 routes; admin header entry on dashboard. scoped tsc 0 errors; 47 screens." },
    { "id": "s8-org-wizard", "title": "Org creation wizard (U): Zustand draft store + 6 steps (basics, branding+group-type, structure+chapters+approval-rule, membership+dues, access+restrictions, review) + publish + success; WizardProgress component; Create FAB on discovery. +8 routes. scoped tsc 0 errors; 55 screens." },
    { "id": "s9-settings-support", "title": "Settings (V): hub + notifications + security (biometric/2FA/change-password) + devices. Support (W): help center/FAQ + ticket list + create + detail thread. +8 routes; profile links. 63 screens. (full scoped tsc re-confirmed 0 errors in session 10.)" },
    { "id": "s10-governance", "title": "Governance & elections (Y): integrated the existing /election feature via app/association/governance.tsx — payment-gated voter eligibility, active-election deep-link, list + admin-setup links; Voting tile on dashboard. Reuse-first (no election screens duplicated). Scoped tsc rc=0; 64 screens." },
    { "id": "s11-admin-depth", "title": "Admin depth (Q): RBAC via useAdminAccess (role+capability flags) — gates home admin entry, admin console, and per-action; member management admin/members list + detail with suspend/restore/transfer/assign-role (confirm + idempotent). +2 routes. Scoped tsc rc=0; 66 screens." },
    { "id": "s12-backend-contract", "title": "Backend contract + go-live wiring: env-driven USE_MOCK (EXPO_PUBLIC_ASSOCIATION_USE_MOCK, default mock) + .env.example; contracts/associations.openapi.yaml (68 paths/76 ops/70 schemas, refs resolve); additive migration 20260628000000_association_module.sql (29 assoc_* tables, money→ledger, audit log); docs/association/GO_LIVE.md runbook + endpoint inventory." },
    { "id": "s13-backend-money-mvp", "title": "Go backend money-path MVP: backend/internal/association (model/service/handler/routes) mirroring internal/groups — PayInvoice (idempotent ledger double-entry member→settlement + revenue split + invoice PAID + audit), GetDues, GetReceipt, DecideApplication; FEATURE_ASSOCIATIONS_ENABLED flag; wired in finance_routes after groups block; revenue-split + idempotency unit tests. Static audit pass (NOT compiled — no Go toolchain)." },
    { "id": "s14-ui-depth", "title": "UI depth batch (7): admin audit-log (Q20); settings language+theme (V18/V19); org-wizard committees; join sub-steps local-branch+committee-interest (B12/B13); paid-event payment (event-pay/[id]); chat depth (image attach, reactions, in-chat search, mute); AI-notes regenerate + export/share. Scoped tsc 0 errors; 70 screens." },
    { "id": "s15-backend-wire-actions", "title": "Go backend: wired routes.go from 4→41 (all read handlers a parallel session wrote, previously unrouted) + added write-path in service_actions.go/handler_actions.go (acknowledge, rsvp/checkin, task status, committee join, event rsvp/register/feedback, offline-payment decision, member suspend/restore/transfer/role — admin-gated + audit). Static audit pass; NOT compiled." },
    { "id": "s16-backend-remaining", "title": "Go backend: wired the remaining ~34 endpoints (routes 41→71): settings prefs/security/preferences/devices, support faqs/tickets/reply, chat threads/messages, AI-notes list/get/status/create/approve/publish/convert, join validate+apply, bulk import preview/confirm, org publish. New model_ext/service_ext/handler_ext + additive settings migration. Static audit pass; NOT compiled." },
    { "id": "s17-backend-reconcile", "title": "Reconciled with concurrent backend edits: full package dup-symbol audit (0 dups across 10 files); wired BulkImportMembers (CSV multipart) at /admin/import/members (gin-safe); fixed migration version collision (settings → 20260629000100, distinct from concurrent assoc_committee_members 20260629000000). Routes 72; package internally consistent; NOT compiled." }
  ],
  "pending_tasks": [
    { "priority": 1, "group": "BACKEND", "title": "Compile + test the Go package in CI (go build/vet/test ./internal/association/...) — the one gate; then add committee_members table + ledger posting for approved offline payments + real bulk-import file parse; real RBAC from me/admin-access; then flip EXPO_PUBLIC_ASSOCIATION_USE_MOCK=false" },
    { "priority": 2, "group": "INFRA", "title": "Reconcile path base (Go /api/v1/finance/associations vs mobile bare /associations) via frontend-web proxy or client base; merge associations.openapi.yaml into shared contracts/openapi.yaml; CI (additive-only migration guard + full tsc + openapi validate); supabase db push on a live env" },
    { "priority": 3, "group": "DEPTH", "title": "Remaining depth polish: chat voice-notes + mentions + WebSocket realtime; AI real audio capture/transcription/diarisation; real PDF export + native share; i18n/theming engine for language+theme prefs; org-wizard logo upload + resume-after-close persistence; chapter-vs-national admin data scoping; suspend-reason modal; real import file picker + column mapping" }
  ]
}
