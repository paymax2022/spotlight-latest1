{
  "session": "Insurance / Protection module (PRD_Paymax_Microinsurance_Integration.md) implemented into existing repo (brownfield) via 5-agent swarm + orchestrator integration",
  "module": "insurance",
  "app_root": "mobile-app/reactnative",
  "backend_root": "backend/internal/insurance (+ provider/mycover, provider/octamile)",
  "admin_root": "frontend-admin/app/admin/insurance",
  "last_updated": "2026-06-26",
  "status": "Built into the existing platform reusing shared services. Partnering-Insurtech model (no underwriting risk). Provider-agnostic underwriter-gateway mirrors MapService; MyCover + Octamile adapters; routing by catalog config. Premium = wallet debit→bind saga with MANDATORY auto-reverse on bind failure; commission on a SEPARATE ledger account; claims payout via wallet credit; embedded binds idempotent on source_event_id; webhooks idempotent on (provider, external_event_id); NDPA consent gate; underwriter+aggregator disclosed. Mobile 44 screens + 20 feature files; admin 21 pages; backend 32 Go files; 2 additive migrations. Mobile + admin tsc=0; backend structural 0 problems; migrations additive-only. Go build/gofmt via insurance-ci.yml (no toolchain in sandbox).",
  "note": "Root task-tracker.json contended by concurrent sessions; this is the authoritative durable Insurance copy.",
  "reuse": "Identity/SSO (User = policyholder), KYC tier gating, wallet ledger (premium debit / claim credit / auto-refund reversing entry), settlement, ledger AccountCommission/AccountProviderClearing, RBAC RequirePermission(insurance.*), route-aggregator pattern, MapService provider abstraction, mobile design tokens + src/components + src/features/payments (PaymentSheet for premium pay), admin _ui kit + sidebar. Feature flags: FeatureInsuranceEnabled (Go) + featureFlags.insurance() (web) + EXPO_PUBLIC_INSURANCE_USE_MOCK + NEXT_PUBLIC_INSURANCE_USE_MOCK.",
  "completed_tasks": [
    { "id": "foundation", "title": "INSURANCE-BUILD-PLAN.md (reuse map + DB/RBAC contract + state machines); FeatureInsuranceEnabled added to Go config; featureFlags.insurance() added to web." },
    { "id": "ib0", "title": "Backend core: underwriter-gateway (interface + Router.Resolve) + MyCover/Octamile adapters (sandbox, keys from env, signature-verify, normalised models); catalog + routing; quote engine; policy state machine; premium-debit→bind saga with auto-reverse + commission on separate account; NDPA consent; insurance_core migration + insurance.* RBAC seed; RegisterInsurance + frontend-web proxy contract." },
    { "id": "ib1", "title": "Backend claims/embedded/webhooks/reconciliation: claim state machine + payout via wallet.Credit (idempotent on claim key); embedded-binding engine (idempotent on source_event_id, maps trip/parcel/bus/consignment/loan/device/wallet/spotlight events → catalog → bind saga); signature-verified webhooks (idempotent on provider+external_event_id); reconciliation + commission ledger; insurance_claims migration; RegisterInsuranceClaims." },
    { "id": "im1", "title": "Mobile buy/policy: Protection hub, browse/detail/disclosure, schema quote form (KYC-prefilled), review/terms, KYC-gap, NDPA consent, premium pay (reused PaymentSheet), bind success/fail+auto-refund, policy wallet/detail/certificate/beneficiaries, renew/cancel/refund. Feature lib + InsuranceColors + components; modules.ts 'Protection' entry. tsc=0." },
    { "id": "im2", "title": "Mobile claims/embedded/agent/partner: claims list/FNOL/form/evidence/status/settled; wallet+device opt-in; shared CoverBadge inline affordance + mini cover view; agent app (8) [policy attaches to customer]; partner/driver app (6). New feature layers reuse IM1. tsc=0." },
    { "id": "ia1", "title": "Admin console (21 pages): dashboard, catalog/routing/schema editors, policy + claim search/detail/timeline, premium tx, commission ledger, reconciliation workbench + break resolution, provider config/event-log/webhook-replay, consent/audit export, refund queue, renewal/lapse monitor, reporting. _ui kit + service + sidebar 'Insurance' section + insurance.* gates. tsc=0." },
    { "id": "integrate", "title": "Wired RegisterInsurance + RegisterInsuranceClaims into finance_routes.go (member /api/finance/insurance, admin /api/insurance/admin, webhooks /internal/webhooks/{mycover,octamile}) under FeatureInsuranceEnabled; frontend-web catch-all proxy /api/v1/insurance/[...path]; mobile modules.ts + admin sidebar (landed); insurance-ci.yml. Verified mobile+admin tsc=0, backend 0 structural problems, migrations additive." }
  ],
  "pending_tasks": [
    { "id": "ci-run", "priority": 1, "blocked_on": "Go+Node runner", "title": "Run insurance-ci.yml: go build/vet/test (saga auto-reverse + idempotency) + web/admin/mobile tsc + additive-migration guard (no Go toolchain / full web tsc in authoring sandbox)." },
    { "id": "embedded-emit-hookups", "priority": 1, "title": "Wire real platform emit points to the embedded engine Handle(): call insurance embedded on transport trip.started/parcel.booked/bus.seat_booked/consignment.created and finance loan.disbursed/device.purchased/wallet.funded + spotlight event_created/contestant.enrolled. Engine + internal /embedded/events route exist and are idempotent on source_event_id; this is the cross-module subscription wiring." },
    { "id": "provider-live", "priority": 2, "blocked_on": "provider contracts (D-1..D-6)", "title": "Replace adapter sandbox TODO(live) endpoints with MyCover/Octamile live API calls; seed catalog from provider sandbox then promote (per-product codes, premium models, sum-insured bands, field schemas, SLAs); set INSURANCE_MYCOVER_*/INSURANCE_OCTAMILE_* + webhook secrets; register webhook URLs." },
    { "id": "go-live", "priority": 2, "blocked_on": "live env", "title": "FEATURE_INSURANCE_ENABLED=true + flip EXPO_PUBLIC_INSURANCE_USE_MOCK / NEXT_PUBLIC_INSURANCE_USE_MOCK off; supabase db push (2 insurance migrations); seed insurance.* RBAC to ops/admin/finance/auditor roles; e2e: voluntary buy (debit→bind→cert), bind-failure auto-refund, FNOL→payout, embedded trip/parcel bind, webhook status sync, reconciliation." },
    { "id": "cert-store", "priority": 3, "title": "Wire the document-store signed-URL provider into the certificate/evidence injection points (left nil-safe in IB0/IB1)." }
  ]
}
