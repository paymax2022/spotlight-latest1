{
  "session": "Hotel Booking / Stays module (PRD_Paymax_Hotel_Booking.md) implemented INTO the existing Property Suite (brownfield) via 6-agent swarm + orchestrator integration",
  "module": "stays",
  "app_root": "mobile-app/reactnative",
  "backend_root": "backend/internal/stays",
  "admin_root": "frontend-admin/app/admin/stays (ops) + frontend-admin/app/extranet (hotelier)",
  "last_updated": "2026-06-26",
  "status": "Built into the Property Suite reusing shared services. Dual-rail supply-gateway (bedbank + direct) mirrors MapService; two-step prebook→book with wallet HOLD→CHARGE→auto-RELEASE saga (settlement/escrow), Naira hotel payouts, commission on separate ledger account, row-locked allotment (no oversell), dedup/mapping, NDPA consent, verified-guest reviews (gated on COMPLETED reservation), FX shown per rate. Mobile 75 screens + 20 feature files (design tokens + components + PaymentSheet reused); ops admin 31 pages; hotelier extranet 35 pages; backend 34 Go files; 2 additive migrations. Mobile + admin tsc=0; backend structural 0 problems; migrations additive-only. Go build/gofmt via stays-ci.yml (no toolchain in sandbox).",
  "note": "Root task-tracker.json contended by concurrent sessions; this is the authoritative durable Stays copy.",
  "reuse": "Property Suite (realtor/property design + map/search components), Identity/SSO (guest/hotelier/agent = capabilities on one User), KYC, finance wallet/ledger/settlement (hold=escrow, charge=settle split, release=refund; Naira payouts; commission AccountCommission separate from AccountProviderClearing), RBAC RequirePermission(stays.*), MapService provider abstraction, mobile src/components + src/features/payments PaymentSheet, admin _ui kit + sidebar, Transport (ride-to-hotel) + Insurance (travel cover) cross-sell, reviews/search/media property-suite infra. Flags: FeatureStaysEnabled (Go) + featureFlags.stays() (web) + EXPO_PUBLIC_STAYS_USE_MOCK + NEXT_PUBLIC_STAYS_USE_MOCK.",
  "completed_tasks": [
    { "id": "foundation", "title": "STAYS-BUILD-PLAN.md (reuse map + DB/RBAC contract + booking saga); FeatureStaysEnabled (Go) + featureFlags.stays() (web)." },
    { "id": "sb0", "title": "Backend core: supply-gateway (Bedbank + DirectInventory adapters, MapService pattern) + search fan-out/dedup; stays-svc domain; reservation orchestrator prebook→PAYMENT_HELD→book→CONFIRMED with wallet hold/charge + auto-release on BOOK_FAILED (settlement escrow/refund); pricing engine (markup/commission/FX); dedup skeleton + mapping queue; NDPA consent; stays_core migration + stays.* RBAC; RegisterStays + frontend-web proxy." },
    { "id": "sb1", "title": "Backend ari/extranet/settlement: ari-svc (rate_day/availability_day/restrictions/promotions, derived rates, bulk edit, FOR UPDATE row-locked allotment → no oversell); hotelier extranet API (object-scoped); Naira payouts (held until completed) + supplier reconciliation + commission ledger; verified-guest reviews (gated on COMPLETED); signature-verified idempotent supplier/ARI webhooks. stays_ari migration; RegisterStaysExtranet." },
    { "id": "sm1", "title": "Mobile discovery+booking (§17 A/B/C/D, 33 screens): search/destination/dates/guests/deals/saved/nearby, results list+map+filters+sort, property/gallery/amenities/location/reviews/rooms/rates/policies, booking flow review→lead-guest→occupants→addons(Transport+Insurance cross-sell)→price-breakdown→payment-method→wallet-pay(reused PaymentSheet)→deposit→promo→confirm(prebook re-price)→processing→failure(auto-release). Feature lib + StaysColors; property-hub Stays→/stays. tsc=0." },
    { "id": "sm2", "title": "Mobile trips/reviews/loyalty/support/agent (§17 E/F/G/H + §20, ~42 screens): booking confirmed+voucher (ride-to-hotel CTA), my-bookings, detail, modify (re-prebook delta), cancel (policy+refund preview), refund status; chat/help/contact/dispute fast-path/notifications; verified review (gated COMPLETED) + sub-scores, loyalty, profile/saved-guests/wallet-overview; agent-assisted (acts on customer identity, PaymentSheet). Reused SM1 lib. tsc=0." },
    { "id": "sa1", "title": "Ops admin (§19, 31 pages): dashboard, supplier/connectivity, mapping/dedup queue, property moderation, content QA, coverage; reservation search/detail + manual actions + refund/dispute fast-path + overbooking; reconciliation/payouts/markup-rules/FX/commission/breaks; loyalty/promotions/reviews/CMS/merchandising; fraud/reliability/agents/KYC; rbac/audit/config/templates. _ui + staysAdminService + sidebar Stays section. tsc=0." },
    { "id": "sa2", "title": "Hotelier extranet (§18, 35 pages): onboarding/verification, content/inventory (room types, rate plans, calendar/ARI month grid, bulk edit, restrictions), promotions/loyalty/visibility/opportunities, reservations/guests/messaging/reviews, finance (Naira payouts/invoices/commission/deposit-recon/bank), analytics, account/staff. Object-scoped hotelier. staysExtranetService + sidebar Extranet section. tsc=0." },
    { "id": "integrate", "title": "Wired RegisterStays + RegisterStaysExtranet into finance_routes.go (member /api/finance/stays, ops admin /api/stays/admin, extranet /api/stays/extranet, webhooks /internal/webhooks/stays-supplier) under FeatureStaysEnabled; frontend-web catch-all proxy /api/v1/stays/[...path]; property-hub Stays pillar repointed; admin sidebar Stays + Extranet sections; stays-ci.yml. Verified mobile+admin tsc=0, backend 0 structural, migrations additive." }
  ],
  "pending_tasks": [
    { "id": "ci-run", "priority": 1, "blocked_on": "Go+Node runner", "title": "Run stays-ci.yml: go build/vet/test (saga auto-release + row-locked allotment + review gating) + web/admin/mobile tsc + additive-migration guard (no Go toolchain / full web tsc in authoring sandbox)." },
    { "id": "ari-allotment-wire", "priority": 1, "title": "Wire SB1's ari.Allotment (AllotmentDecrement/Release, FOR UPDATE) into the SB0 reservation saga for the DIRECT rail so book-time decrement runs (SB0 left TODO(SB1)); Rail A (bedbank, Phase 1) needs no allotment. Inject ari service into the reservation service at registration." },
    { "id": "supplier-live", "priority": 2, "blocked_on": "D-1 supplier choice + contracts", "title": "Replace BedbankAdapter sandbox TODO(live) with the chosen Rail-A supplier (RateHawk/ZentrumHub) live API + content sync; seed catalog/content; set supplier keys + webhook secrets + register webhooks. Resolve D-2 (markup/commission), D-3 (FX source/spread), D-4 (payout cadence), D-7 (dedup threshold)." },
    { "id": "go-live", "priority": 2, "blocked_on": "live env", "title": "FEATURE_STAYS_ENABLED=true + flip EXPO_PUBLIC_STAYS_USE_MOCK / NEXT_PUBLIC_STAYS_USE_MOCK off; supabase db push (2 stays migrations, PostGIS); seed stays.* RBAC to ops/hotelier/agent/finance/auditor roles; e2e: search→prebook(re-price)→book(hold→charge), book-failure auto-release, cancel→instant refund, direct-rail oversell-blocked, extranet calendar/ARI + Naira payout, verified review." },
    { "id": "voucher-store", "priority": 3, "title": "Wire document-store signed-URL provider into voucher/invoice/media injection points (left nil-safe)." }
  ]
}
